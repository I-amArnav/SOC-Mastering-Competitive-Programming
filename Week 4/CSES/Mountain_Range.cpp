#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
#define fori(n) for(int i = 0; i < n; ++i)
#define forj(n) for(int j = 0; j < n; ++j)
ll mod=1e9+7;

//incomplete!!!!

int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    int n,done=0;cin>>n;
    vector<ll>h(n+1),dp(n+1);
    vector<bool>dealt(n+1,false);
    for(int i=1;i<=n;i++){
        cin>>h[i];
    }
}